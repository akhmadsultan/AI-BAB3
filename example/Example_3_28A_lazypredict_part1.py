# Example 3.28a - install LazyPredict (Google Colab / Jupyter cell)
# Note: the book prints "lazypredit" here (typo) but its own text a few
# paragraphs earlier gives the correct package name: lazypredict
#!pip install lazypredict
